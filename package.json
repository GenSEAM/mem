{
  "name": "@genseam/asl-mem",
  "version": "0.3.0",
  "description": "Zero-server in-memory vector database, semantic knowledge graph, and ADR/Invariant Records ledger in ASL",
  "type": "module",
  "main": "dist/mem.js",
  "scripts": {
    "test": "node benchmark/run.js --check",
    "benchmark": "node benchmark/run.js"
  }
}
