{
  "name": "@genseam/asl-mem",
  "version": "0.3.0",
  "description": "Zero-server in-memory vector database, semantic knowledge graph, RingBuffer streaming, and WAL/Snapshot storage substrate in ASL",
  "entry": "src/engine.asl",
  "main": "src/engine.asl",
  "targets": {
    "wasm": {"output": "dist/mem.wasm"},
    "ts": {"output": "dist/mem.ts"},
    "py": {"output": "dist/mem.py"}
  }
}
